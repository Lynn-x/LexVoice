# LexVoice 2.3.1

发布日期：2026-09-12

这次主要调整纪要的阅读层次，以及大纲里的说话人区域。

## 原始材料默认折叠

- 新整理的笔记中，纪要正文保持展开。录音信息、录音过程大纲、实时转写草稿、原始音频和完整转写分别收进 Obsidian 原生折叠块，点标题即可展开。
- 个人笔记、综合纪要、学习、面试和晋升答辩等模式统一使用这套呈现。文本导入的原文、面试提纲和晋升初审也分别折叠。
- 空的实时草稿不再重复显示几十条“此段无内容”；真正的转写、待重试标记和原始音频保留。
- 原始材料里的分段标题改为加粗标签，Obsidian 大纲导航保留纪要的主题层级。

## 录音大纲与说话人

- 录音大纲展开后，左侧是时间，右侧是要点。长句自然换行，保留子项缩进和回听链接，不在大纲里铺开音频文件名。
- 旧笔记中的 HTML 大纲仍可显示为时间轴。
- 说话人回到“大纲”中，可以折叠、关闭和重新打开，不再单独占一个 Tab。
- 已有会后完整转写时，说话人列表优先读取正式结果，不混入实时草稿的旧编号。模型自身识别出的多余编号仍需要核对。

## 兼容与升级

- 折叠内容仍可用于重新整理、续录、失败重试和说话人修改。修复重试定位和折叠后姓名替换的兼容问题。
- 更新不会批量改写旧笔记，也不会删除原始转写。纯转写模式保留原文阅读入口。
- 本版没有调整 ASR 服务的文件限制或切分策略，也没有新增自动双链功能。

安装时替换 `<你的知识库>/.obsidian/plugins/lexvoice/` 下的 `main.js`、`manifest.json`、`styles.css`，再重新加载插件。保留原来的 `data.json` 和笔记文件。

安装产物在 [Lynn-x/LexVoice](https://github.com/Lynn-x/LexVoice/releases) 发布；开发源码仍为私有，许可证未变。
