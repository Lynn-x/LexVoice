# LexVoice 2.3.0

发布日期：2026-09-11

这次增加了会前材料入口和独立的“说话人”页，调整了纪要整理方式，并修复了部分转写服务测试通过、实际使用失败的问题。

## 会前材料

- 侧边栏顶部新增可折叠的材料区，可在录音前创建会议笔记，粘贴文字、拖入图片或 PDF，也支持 Markdown 和 TXT 附件。
- 提取的与会人写入笔记属性，主题和议程写入正文；待解决事项由自己填写。材料、实时大纲和最终纪要保存在同一篇 Markdown 中。
- 开启“用于转写与整理”后，名单和背景可用于理解发言、核对名称及整理纪要。会前填写的问题会逐项回应；没有单列问题时，可按明确列出的议程核对。
- 回应保留转写原文依据。没有找到可核验依据的事项显示“待核对”，会前计划不会自动变成会上结论。
- 支持带括号的模板标题和自定义背景章节，保留手工编辑的会前材料。

## 说话人与侧边栏

- 说话人核对移到独立页，不再同时占用大纲和沉淀区域。
- 已有会前与会人名单时，可直接选择姓名配对，也能补充姓名、合并同一人的编号或修正单句归属。
- 实时大纲支持选中、复制文字；桌面端可从纪要列表拖出文件，按住 Alt 拖出路径。能否接收文件取决于目标软件。
- 纪要列表优先显示标题，“导入”等来源信息移到标签中；会前材料区、设置首页和窄窗口布局更紧凑。

## 整理与转写

- 调整大纲和纪要提示词：明确的结论先写，再展开依据、条件和分歧；保留录音中有价值的计划外议题，会前材料不限制正文范围。
- 沉淀提取排除会前材料区，避免把准备阶段的计划提取为会议产出。
- 修复部分短 M4A 音频使用 XingChen 说话人分离时返回 400 的问题，按模型要求转换为 16 kHz 单声道 WAV；保留原始录音。
- 修复流式服务被当作普通 HTTP 接口测试的问题，按实际任务配置验证连接。
- 修复阿里百炼原生模型列表读取与分页、会后转写地址兼容问题；保留用户配置的工作空间和地域地址。
- 模型服务页保留连接测试结果，并提供进入对应任务模型设置的入口。移除音频设备名旁的推测性标签。

## 升级与提醒

下载本 Release 的 `main.js`、`manifest.json`、`styles.css`，替换 `<你的知识库>/.obsidian/plugins/lexvoice/` 中的同名文件，再重新加载插件。保留原有 `data.json` 和知识库文件。

- 会前材料的模型使用默认关闭。读取普通文本材料、图片或扫描 PDF，以及将材料用于转写和整理，都需要授权；使用云端模型时，相关内容会发送给所选服务商。规范 Markdown 模板可在本地读取。
- 图片和扫描 PDF 需要支持图像输入的 AI 整理模型；Word 文档请先导出为 PDF。
- 服务测试通过只代表测试请求可用，不能保证任意音频的格式、时长、大小都符合模型要求。说话人识别与姓名配对仍需人工核对。
- 更新不会自动重写已有纪要。需要应用新的整理方式时，请手动重新整理。

安装产物继续在 [Lynn-x/LexVoice](https://github.com/Lynn-x/LexVoice/releases) 发布，开发源码不公开。沿用现有[软件许可](https://github.com/Lynn-x/LexVoice/blob/main/LICENSE)，历史 MIT 和第三方声明保留不变。
